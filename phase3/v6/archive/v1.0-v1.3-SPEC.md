# Hermem V6 规划：从"主动检索"走向"情境感知"

**版本**: v1.0
**日期**: 2026-06-06
**状态**: 草案（待评审；Sprint 0 立项）
**依据**:
- oGMemory（openGauss）评估报告 2026-06-05（评估对 Hermem 的参考意义）
- 既有 `Hermem V5后续版本规划`（V6.0 节草案）
- 2026-06-01 阈值调整落地（HIGH=0.70, MEDIUM=0.50，搜索 tier 已用 config 常量）

---

## V6 核心目标

**从"按固定频率主动检索"走向"按需、预测性、可解释的情境感知"**，并补上 Hermem 长期缺位的"评测闭环"。

V5 已经解决："对话过程中能不能主动找到相关历史记忆？"——能，但触发是机械的（N 回合一次）、召回是裸露的（直接输出 `[自动回忆 - 相似度 0.91]`）、评测是缺失的（没指标回答"这套系统有没有用"）。

V6 要解决三件事：
1. **按需触发** —— 不是每 N 回合就触发，而是真的"需要"时才触发
2. **预测性** —— 不仅匹配用户字面问题，还要预判用户**接下来可能需要什么**
3. **可解释** —— 召回内容用自然语言过渡句包装，不再裸露相似度数字

并补一条贯穿全期的横切关注点：
4. **可评测** —— 从"看人工 review"升级到"看指标"，给 V7+ 留出数据基础

---

## 架构现状与缺口分析

### V5 现状（2026-06-01 audit-clean）

```
用户消息（每 N 回合）
    ↓
bge-m3 主动检索（固定频率，ACTIVE_RETRIEVAL_FREQUENCY=3）
    ↓
search_with_tier → 高置信(≥0.70) 注入 / 中置信(0.50–0.70) 累积
    ↓
裸露输出：[自动回忆 - 相似度 0.91] + chunk 内容
    ↓
Agent 处理 → 响应
```

| 能力 | 状态 |
|---|---|
| 主动检索（频率触发） | ✅ V5 已实现 |
| 分层阈值 | ✅ HIGH=0.70 / MEDIUM=0.50（config.py 落地） |
| 注入防重复 | ✅ _injected_chunk_ids |
| 0.85 硬编码 | ✅ **主路径已修**，仅 `templates/__init__.py:1607` 的 import-fallback 兜底保留 0.85 字面量（详见 Sprint 0 / 风险章节）|
| **按需触发** | ❌ V5 机械 N 回合触发，不看 LLM 置信度/意图 |
| **预测性召回** | ❌ 没有"猜测用户接下来要问什么"的能力 |
| **可解释包装** | ❌ 直接输出相似度数字 + chunk 原文，突兀 |
| **评测框架** | ❌ 只有 `hermem_stats` 工具（4 个基础项）+ `hermes memory health`（基础设施检查），**没有命中率/token 趋势/抽取去重率**等使用指标 |

### 关键事实澄清（评估报告与代码现状的差异）

评估报告 2026-06-05 写"search_with_tier 0.85 硬编码 bug 还在"。**代码核查后此结论需要修正**：

| 位置 | 状态 |
|---|---|
| `phase3/impl/config.py:327` | ✅ `ACTIVE_RETRIEVAL_THRESHOLD_HIGH = 0.70`（V5.5 阈值已落地）|
| `phase3/impl/vector_search.py:105-140` `search_with_tier()` | ✅ 使用 `config.ACTIVE_RETRIEVAL_THRESHOLD_HIGH` 常量，无硬编码 |
| `templates/__init__.py:1607` | ⚠️ `from impl.config import ... except: threshold = 0.85` —— import 失败时的兜底分支仍写 0.85 字面量 |
| `Hermem-V5-TODO.md:304-308, 826-827` | ⚠️ **文档未同步**，仍写 0.85/0.65（这是文档债，不是代码 bug）|

**结论**：`search_with_tier` 0.85 硬编码问题在 2026-06-01 阈值调整中已修。Sprint 0 只需清理两处残留（templates fallback 字面量 + 顶层 TODO 文档），不要再"修主路径"。

---

## V6 目标架构

```
用户消息
    ↓
[按需触发判断] ← 意图切换概率 / LLM logit 置信度 / 显式 keyword 命中
    ↓ 触发
1. 显式检索（精确匹配 + 1 跳"已有候选"防重复）  ← 来自 oGMemory §6 ReAct 抽取的启发
    ↓
2. 预测性召回（基于 L3 画像 + 对话上下文，预测下一步需要的记忆）
    ↓
[可解释包装] ← 模板 + 小模型 (qwen2.5:1.5b) 生成自然语言过渡句
    ↓
注入（带过渡句，不再裸露相似度）
    ↓
Agent 处理 → 响应
```

### 四大能力模块

#### 模块 1：按需触发（Dynamic Injection）

**问题**：V5 固定 N 回合触发，浪费算力 + 错过关键时刻。

**方案**：在以下任一信号出现时立即触发主动检索：
- 意图分类置信度低于阈值（`INTENT_CONFIDENCE_THRESHOLD = 0.7`）
- 同一 chunk 在最近 3 轮内被累积到中置信但未注入（暗示话题持续）
- 用户消息中存在显式 anchor 关键词（如"上次"、"之前那个"、"你还记得"）
- 任务进入新阶段（当前 disposition 的 `error_count > 0` 累积时）

**降级**：若 LLM 置信度信号获取成本高（logit 不暴露），降级用"每 N 回合 + anchor 关键词提前触发"。

#### 模块 2：预测性召回（Predictive Recall）

**问题**：召回是被动的——用户问 A，召回关于 A 的记忆。V6 要主动猜测 B/C。

**方案**：
1. 读取 L3 用户画像（user_profile.md）+ 当前对话上下文（前 3 轮）
2. 用轻量级 LLM（`qwen2.5:1.5b`）生成 2-3 条预测性查询词
3. 对每条查询词执行向量检索（threshold=MEDIUM）
4. 合并去重，与显式检索结果取并集

**示例**：用户说"帮我安排明天的行程" →
- 显式：检索"行程安排"相关
- 预测：检索"常去的地点"（L3 画像）+ "历史抱怨过的拥堵路线"（disposition）+ "用户通常午休时间"（pattern）

**接口**：`hermem_search_predictive(context_window, user_query) -> list[chunk]`，与现有 `hermem_search` 平行。

#### 模块 3：可解释包装（Explainable Wrapper）

**问题**：当前输出是 `[自动回忆 - 相似度 0.91]\n- chunk 内容`，突兀且不友好。

**方案**：在注入前调用 `explain_chunk(chunk, current_query)` 生成自然语言过渡句。
- **轻量路径**（默认）：模板拼装，4-6 个固定句式轮转
- **增强路径**（可选）：调 `qwen2.5:1.5b` 生成，但限定 latency ≤ 200ms

**示例**（对比）：

| 旧（V5）| 新（V6）|
|---|---|
| `[自动回忆 - 相似度 0.91]\n- 上次 cron 任务失败的根因是 ...` | `看到您提到 cron 任务，我想起上周一次类似失败的排查记录（根因是 launchd 路径配置问题）。需要我展开讲吗？` |

**设计原则**：
- 过渡句不掩盖相似度（仍可在 footer 注明 `[内部召回 · 相似度 0.91]`，但默认不强制展示）
- 过渡句不臆造内容（不能"为了过渡"添加 chunk 里没有的细节）
- 失败时降级到 V5 格式（不阻断流程）

#### 模块 4：可评测性（Eval Framework）

**问题**：Hermem 没有指标回答"召回率多少 / token 消耗趋势 / 抽取去重率"。

**方案**：

**最小指标集**（评估报告 V5.5 §"1+2"要求的 + V6 必须有的）：

| 指标 | 定义 | 数据源 |
|---|---|---|
| `total_chunks` | chunks 表总行数 | `chunks` |
| `embedding_coverage` | 有 vec_index 的 chunk 占比 | `chunks.vec_index IS NOT NULL` |
| `hit_rate` | 召回过 1+ 次的 chunk 占比（30 天窗口）| `usage_count > 0` |
| `avg_inject_token` | 单次注入平均 token 数 | inject 日志（需新增）|
| `dedup_rate` | 抽取后被判定为重复/合并的占比 | 抽取日志（需新增）|
| `recall_at_5` | 离线评测：标注文档中 top-5 含正确答案的比例 | 需新建标注集 |

**实现路径**：
- **立即可加**（不需新数据）：`total_chunks` / `embedding_coverage` / `hit_rate`（用现有 `usage_count`）
- **需新建日志**：`avg_inject_token`（V5 inject 路径加 log）/ `dedup_rate`（V5.5 disposition 提取加 outcome 字段）
- **评测框架**（V6 末期）：离线 `hermes memory eval --against labels.jsonl`，跑 recall@5 / MRR

**新增 CLI**：`hermes memory stats` 子命令（目前 CLI 只注册了 `health` 和 `rebuild`，**没有 `stats`**——这是评估报告里写错的事实，需在 Sprint 0 创建）。

---

## 实施拆分（5 个 Sprint）

### Sprint 0：基础设施与可观测（**本计划范围，Oliver 拍板 1+2**）

- **任务 0.1**：清理 `templates/__init__.py:1607` 的 0.85 fallback 字面量 → 改为 `ACTIVE_RETRIEVAL_THRESHOLD_HIGH` 常量失败时的回退日志 + 安全值 0.50（不注入，但记录到中置信累积）
- **任务 0.2**：更新顶层 `Hermem-V5-TODO.md` 的 0.85/0.65 文档 → 标注 V5.5 已对齐 0.70/0.50
- **任务 0.3**：新增 `hermes memory stats` CLI 子命令，输出 6 个最小指标（total_chunks / embedding_coverage / hit_rate / avg_inject_token / dedup_rate / ollama_latency）
- **任务 0.4**：在 V5 active retrieval 注入路径加 `avg_inject_token` 日志（轻量，不破坏现有流程）

详见 `TODO.md` Sprint 0。

### Sprint 1：按需触发（Dynamic Injection）

- **任务 1.1**：在 `intent_classifier` 路径加置信度暴露（返回 `confidence` 字段）
- **任务 1.2**：实现 `_v6_should_trigger()`：综合意图置信度 + 中置信累积 + 显式 anchor 关键词
- **任务 1.3**：将 `_v5_active_retrieval()` 的固定频率触发替换为 `_v6_should_trigger()` 调用
- **任务 1.4**：保留固定频率作为兜底（防止意图分类失败时无检索）

### Sprint 2：预测性召回（Predictive Recall）

- **任务 2.1**：实现 `hermem_search_predictive()` 接口（独立于 `hermem_search`）
- **任务 2.2**：用 `qwen2.5:1.5b` 生成预测性查询词（限定 latency ≤ 200ms，失败时降级为空）
- **任务 2.3**：与显式检索结果合并去重
- **任务 2.4**：评测：对比"仅显式 vs 显式+预测"在历史 session 上的命中率

### Sprint 3：可解释包装（Explainable Wrapper）

- **任务 3.1**：实现 4-6 个固定过渡句模板（中文优先，与 V5 `[自动回忆]` 标签融合）
- **任务 3.2**：实现 `explain_chunk()` 轻量路径（默认走模板）
- **任务 3.3**：实现 `explain_chunk()` 增强路径（可选调 qwen2.5:1.5b，加 latency 监控）
- **任务 3.4**：在 V6 inject 路径调用 `explain_chunk()`，失败降级到 V5 格式

### Sprint 4：评测框架（Eval Framework）

- **任务 4.1**：构建人工标注文档（30-50 条历史 session，标注 ground-truth chunk id）
- **任务 4.2**：实现 `hermes memory eval --against labels.jsonl`，跑 recall@5 / MRR
- **任务 4.3**：纳入周报告（`~/.hermes/memory/eval/weekly/`），跟踪趋势
- **任务 4.4**：自动回归——每次 merge 前跑 eval，差过阈值则 fail CI

---

## 验收标准

### Sprint 0（必达，Oliver 拍板 1+2）

1. `grep -r "0\.85" ~/.hermes/projects/hermem/ --include="*.py"` 输出仅剩历史/文档语境，无活动代码路径
2. `hermes memory stats` CLI 可执行，输出 6 个指标，无报错
3. `total_chunks` / `embedding_coverage` / `hit_rate` 三个指标基于现有数据可立即算出（无需新数据采集）
4. `avg_inject_token` 需要至少 1 次主动注入事件后才能有值（日志就位即可，数值观察在 Sprint 1 后）
5. `hermes memory health` 仍然通过，V5 active retrieval 不被破坏

### Sprint 1-4（待 Sprint 0 完成后另立 spec 细化）

每个 Sprint 单独跑通后写一份 sprint-summary.md 追加到 `phase3/v6/eval/`，不混进本 SPEC。

---

## 风险与权衡

| 风险 | 严重度 | 缓解 |
|---|---|---|
| 0.85 fallback 兜底代码删除后，import 失败时无阈值可用 | 低 | 改为日志告警 + 用 0.50 兜底（记录到中置信累积）|
| `avg_inject_token` 需新日志路径，可能影响 V5 性能 | 低 | 日志写入异步队列，不在 inject 主路径阻塞 |
| `qwen2.5:1.5b` 调用延迟不稳（> 200ms）| 中 | Sprint 2/3 默认走模板路径，LLM 路径 opt-in；超时降级 |
| 评测标注集构建成本 | 中 | Sprint 4 任务 4.1 由 Oliver 主导，AI 提供候选标注 + 人工 review |
| LLM logit 不可用（OpenAI/Claude 风格 API 多数不暴露）| 高 | 按需触发主信号降级为"意图置信度 + anchor 关键词 + 中置信累积"三件套，不强依赖 logit |

### 已知不做的事（明确划出去）

- **ContextFS 关系层 / 记忆图谱**：oGMemory §3 评估里写"low ROI, 押注未来"，**V6 不做**。留给 V7+。
- **YAML Schema 驱动记忆类型**：oGMemory §4 评估里写"中 ROI，等 V6 稳了再动"。**V6 不做**。
- **多用户/多 Agent/多空间隔离增强**：CLAUDE.md 显示已有 `namespace + person_id` 基础，V6 不动。

---

## 与 V5 / V5.5 的关系

- **V5**（已实现）：主动检索 + 分层阈值 + 防重复
- **V5.5**（已实现）：L4 反思 + 冲突消解 + 主动遗忘
- **V6**（本计划）：按需触发 + 预测性召回 + 可解释包装 + 评测框架

V6 不改 V5/V5.5 现有接口，**叠加**而非**替换**。Sprint 0 完成后 `_v5_*` 路径仍然有效；Sprint 1-3 引入 `_v6_*` 新路径，由 `_v6_should_trigger()` 决定走哪条；Sprint 4 评测框架独立于检索路径。

---

## 决策待办

- [ ] V6 路径选择：phase3/v6/（本草案）vs 顶层 Hermem-V6-SPEC.md + Hermem-V6-TODO.md（仿 V5 顶层）— **默认 phase3/v6/**，理由：与 V5.5 对齐，自包含；如需顶层文件可加软链
- [ ] Sprint 0 任务 0.1：0.85 fallback 改 0.50 是否过激进？— **建议保守**：保留 0.85 但加 WARNING 日志（"import 失败回退到 V5.0 旧阈值，bge-m3 实测会无命中"），让异常更显眼
- [ ] Sprint 1 anchor 关键词词典：手动维护 vs LLM 提取？— **建议手动维护**（5-10 个固定词），LLM 提取容易漂移

---

*v1.0 草案；Sprint 0 立项后状态变更为 "Sprint 0 in progress"。*

---

## v1.1 追加（2026-06-06 晚 — MemPalace 借鉴 + 基础设施 bug 暴露）

**追加背景**：v1.0 落定后，后续 4-2-1-3 收口验证 + MemPalace 评估 + 多次 hermem_search 卡顿复盘，发现三类**v1.0 未覆盖**的事项。按"方案 B 流程"——**保留 v1.0 不动，在文末追加 v1.1 章节**，等所有讨论稳定后再做最终融合。

### 1. MemPalace 借鉴要点（高优先级 — 评估通过的具体借鉴）

**MemPalace**（MemPalace/mempalace，54k stars，9 周龄）已通过完整评估（来源：2026-06-06 MemPalace 评估报告）。从源码读出的、**对 V6 有具体落地点**的借鉴：

| 借鉴点 | MemPalace 实际做法 | V6 落地建议 | 优先级 |
|---|---|---|---|
| **Hybrid 检索公式** | `score = 0.6 × vec_sim + 0.4 × bm25_norm`，BM25 在候选集内算 IDF，min-max 归一 | V6 Sprint 1/2 替换当前 `search_with_tier` 硬阈值；`vec_sim` 用绝对 cosine（`max(0, 1 - dist)`）避免加减候选重排 | P0（V6 核心） |
| **后端 ABC 抽象** | `BaseCollection` + `BaseBackend` + typed `QueryResult`，4 个实现（chroma/pgvector/qdrant/sqlite_exact） | V6 P1 抽 `MemoryStore` 协议；当前 SQLite+FTS5 是唯一实现 | P1 |
| **50/450 held-out split** | `lme_split_50_450.json`（seed=42），50q 反复调，450q 只跑一次 | V6 Sprint 4 评测框架必须遵循；防阈值校准 overfit | P0（评测可比性） |
| **评测透明披露** | `BENCHMARKS.md` 758 行自陈 "teaching to test" + 主动撤回 100% headline | V6 Sprint 4 评测文档模仿（轻量版）：每次评测记录 dev 调过什么 / held-out 一次性跑多少 | P1 |
| **隔离契约** | `PalaceRef` docstring 显式"MUST NOT" + `test_backend_conformance.py` | V6 P2 写一段 contract docstring + 1 个 `test_isolation.py` | P2 |
| **错误层次** | `BackendError → PalaceNotFoundError → CollectionNotInitializedError`（多继承保 legacy） | V6 P2 顶 6 类错误；**不**学多继承保 legacy | P2 |

**不借鉴的（明确划出）**：
- Verbatim 哲学（Hermem 已分 4 类，密度更高）
- Palace/Wing/Room/Drawer 命名（营销味重）
- 29 个 MCP tools（Hermem <10 个，不扩）
- LLM rerank 100% 路径（违反"本地/无 API key"定位）
- `develop` 当默认分支（行业反模式）
- 版本号 v3.0 → v3.4 / 2 个月节奏（过快）

### 2. 0.85 fallback 验证结论（v1.0 决策 2 已被事实推翻）

**v1.0 决策 2 原文**："`templates/__init__.py:1607` 是 import 失败兜底分支，0.85 字面量在该分支保留 + WARNING + counter"

**v1.1 验证结果**（2026-06-06 晚真实核查）：
- `phase3/impl/templates/` 目录**不存在**（只在根 `/Users/oliver/.hermes/projects/hermem/templates/__init__.py` 有孤立文件）
- `vector_search.py:105-140 search_with_tier()` 使用 `config.ACTIVE_RETRIEVAL_THRESHOLD_HIGH` 常量，**无 0.85 字面量**
- `config.py:327` 已写 `ACTIVE_RETRIEVAL_THRESHOLD_HIGH = 0.70`（V5.5 调整落地）
- 0.85 字面量**仅剩 1 处**：`config.py:24 SIM_THRESHOLD_MERGE = 0.85`（L2 scene 合并用，**不是 active retrieval 路径**）

**v1.1 决策更新**：
- ❌ **决策 2 整项作废** — 前提不成立（文件不存在、阈值已修）
- 🆕 **新增 P2 任务**：`SIM_THRESHOLD_MERGE = 0.85` 加 daily metric counter，先观测触发次数再决定是否改值（"先观测再修"原则）
- v1.0 TODO Step 1 任务 0.1（0.85 fallback 改造）**整体作废**

### 3. 新增基础设施 bug（v1.0 未覆盖的真实 blocker）

| Bug | 触发场景 | 当前状态 | V6 Sprint 0 落地 |
|---|---|---|---|
| **3 个僵尸 hermes 进程** | 占着 hermem.db fd + .vector_write.lock；下次 Ollama 调度卡时导致 hermem_search hang 30+ 分钟 | 已知未修（PID 4417/73745/39006）| Sprint 0 任务 0.6：启动时检测 + 自动 kill（**不依赖人**） |
| **Ollama server 全部写端点 hang** | bge-m3 embedding cold-start hang >10min；generate 12s 冷加载（已知限制）；`/api/embed` + `/api/chat` 全部 hang | 已知（"基础设施 blocker"反复出现）| Sprint 0 任务 0.7：客户端加真 timeout（`requests.post(timeout=30)` 替换 `timeout=120` 装饰品）|
| **`embedding.py:62 timeout 死参数`** | 代码写 `timeout=120` 但客户端实现是 streaming-style 不真生效 | 已知 | Sprint 0 任务 0.7 合并修 |
| **V4.5 uncommitted 改动**（utils.py + journal.py）| 139 行 MiniMax 凭据解析 + env file 加载，预先存在未提交 | 已知 | **Sprint 0 启动前置** — 必须先 commit，否则 Sprint 0 测试会有污染 |

### 4. v1.1 对 v1.0 决策待办的影响

| v1.0 决策 | v1.0 状态 | v1.1 状态 |
|---|---|---|
| 决策 1（路径 phase3/v6/）| 待拍板 | **仍待拍板**（无新论据）|
| 决策 2（0.85 fallback）| 待拍板 | **❌ 作废**（前提不成立）|
| 决策 3（anchor 词典）| 待拍板 | **仍待拍板**（v1.1 仅讨论实现细节，决策本身不变）|

### 5. v1.1 → 待融合（最终版前需要做的事）

1. 把 §1 MemPalace 借鉴点写入 Sprint 0 任务表
2. 把 §2 0.85 fallback 作废结论写入"决策待办"段（标 ❌ resolved in V4.5）
3. 把 §3 基础设施 bug 写入 Sprint 0 新增任务（0.6 / 0.7）
4. Sprint 0 启动前置条件更新为：V4.5 uncommitted 已 commit + Ollama 健康 + 僵尸进程清理
5. 融合时机：3 个待决策全部拍板 + 当前所有讨论稳定后

*v1.1 追加；v1.0 草案保留作为决策上下文。最终融合前不删除。*

---

## v1.2 追加（2026-06-06 — Memory Box 讨论的方法论借鉴）

**追加背景**：v1.0/v1.1 落定后，2026-06-04 Memory Box C 端产品使用反馈与定位讨论 3 个 session（讨论笔记 `~/.hermes/knowledge/thoughts/2026-06-04-memorybox-使用反馈.md` + 借鉴分析 `~/.hermes/knowledge/tech/2026-06-06-memorybox-discussion-borrowing-to-hermem.md`）暴露的 4 类**对 V6 有具体落地点**的借鉴。按"方案 B 流程"——**追加 v1.2 章节**，等所有讨论稳定后再做最终融合。

**关键判断**（与 v1.1 借鉴评估同样来源，但维度不同）：
- MemPalace 借鉴 = **工具层面**（hybrid 公式、ABC 抽象、split 方法论）
- Memory Box 借鉴 = **方法论层面**（长期记忆 + 误差驱动学习思想的独立验证）
- 两者**互补**，不互斥。Memory Box 借鉴给"V6 是否要往"长期人格 + 行为数据"方向走"提供了 C 端产品层的独立验证。

### 1. 立即可借鉴（4 个 — 全部加速 V6 已有方向）

| 借鉴点 | Memory Box 讨论原话 | Hermem V6 对应物 | 落地 Sprint | 优先级 |
|---|---|---|---|---|
| **1.1 LLM 不决策只生成** | "LLM 负责生成推送内容，不负责决定推不推" | V6 Sprint 1（按需触发）已实现"结构化信号加权 + LLM 改写"分离 | Sprint 1 文档化 | P1 |
| **1.2 行为/声明 gap 是金矿** | "用户说的和做的之间有什么 gap 才是推送的金矿" | V6 缺 disposition conf 自动更新（当前 conf=0.8 硬编码）| **Sprint 5 候选** | **P0** |
| **1.3 长期人格一致性** | "6 个月后不带提示接续对话" | V6 L1 fact_extraction 没加"用户角色特征"字段 | **Sprint 5 候选** | **P0** |
| **1.4 情感重量公式** | "recency × emotional_intensity × pattern_relevance 加权" | V6 已计划 `emotional_valence + arousal` 字段（V4.4 P0 推到 V6）| Sprint 1/2 吸收 | P0 |

**Sprint 候选 5.1：`disposition` conf 自动更新**（来自借鉴点 1.2）
- 输入：recall 后用户 follow-up 行为（用了 / 忽略了 / 反驳了）
- 输出：disposition 的 conf 值从硬编码 0.8 → 由 recall_outcome 行为信号驱动
- 实现路径：依赖 Sprint 5 候选 5.2（recall_outcome 表），合并设计
- 预估：半天（schema 改 + disposition_updater 改 + 测试）

**Sprint 候选 5.2：`recall_outcome` 表 + `behavior_log`**（来自借鉴点 1.2 衍生）
- 目的：补 Hermem 长期缺位的"行为数据源"（Memory Box 三数据源闭环中 Hermem 缺的那一环）
- 表结构：
  ```sql
  CREATE TABLE recall_outcome (
      recall_id TEXT PRIMARY KEY,
      session_id TEXT,
      chunk_id TEXT,
      follow_up_type TEXT,        -- 'used' / 'ignored' / 'rejected'
      follow_up_turn_count INT,   -- 这次 recall 后用户又聊了几轮
      created_at TIMESTAMP
  );
  ```
- 数据源：V5 active retrieval 注入点 hook（chunk_id 已知），Sprint 1+ 接入
- 副作用：反哺 5.1 disposition conf 自动更新
- 预估：1-2 天（schema 迁移 + hook + 测试）

**Sprint 候选 5.3：L1 fact_extraction 加"用户角色特征"字段**（来自借鉴点 1.3）
- 目的：支撑"6 个月后接续对话"——这是 Memory Box 评估里"长期人格一致性"在系统层的对应物
- 新增字段：
  - `user_role`：用户角色（产品经理 / 工程师 / 创作者 / ...）
  - `user_style`：表达风格偏好（直接 / 详细 / 数据驱动 / ...）
  - `user_values`：核心价值观（追求效率 / 注重质量 / 重视隐私 / ...）
- 实现路径：改 `phase3/impl/l1_extract.py` 的 prompt（加输出字段）+ l1_facts 表 schema 迁移
- 预估：1 天（prompt 改 + schema 加列 + 测试）
- 与现有 disposition 的区别：disposition 是 IF/THEN 行为模式，user_role/style/values 是稳定的身份特征

### 2. 可改造借鉴（3 个 — 翻译场景后落地）

| 借鉴点 | Memory Box 原方案 | Hermem 翻译 | 落地 Sprint |
|---|---|---|---|
| **2.1 价值向量 V(t)** | 用户维度关切度持续更新，每次行为微调 | concept_tag 加 "关注度" 维度（0-1），用 recall 行为自动调整，半衰期 7 天 | **Sprint 5 候选** |
| **2.2 奖励预测误差（多巴胺层）** | 跨多次行为的全局信号 | `recall_outcome` 累积 → 算"这块记忆对用户的真实价值" | 与 5.2 合并 |
| **2.3 教练 vs 管家人格** | "我注意到你 3 月想做的 Y，现在看 X 可能是新角度" | L1 fact_extraction 不重写意图，保留原始引用 | **Sprint 2 增强** |

**Sprint 候选 5.4：concept_tag 关注度加权**（来自借鉴点 2.1）
- 当前：concept_tag 是离散标签
- 升级：给 concept_tag 加"关注度"维度（0-1 范围），用 recall 行为自动调整，半衰期 7 天
- 加权公式：
  ```python
  score = cosine_sim × recency × concept_weight × pattern_relevance
  ```
- 预估：1 天（schema 加 concept_weights 字典 + 加权公式改 + 测试）
- 依赖：5.2 recall_outcome 表

**Sprint 候选 5.5：fact_extraction 保留原始引用**（来自借鉴点 2.3）
- 目的：保留用户语气和立场，未来 AI 拿完整信息
- 实现：`l1_facts` 表加 `raw_quote` TEXT 字段，存原始引用（如 "用户 2024-03 说'X 看起来有意思'"），AI 不重写为 "用户认为 X 是好的"
- 副作用：和现有 L4 reflection 区分（L4 是结构化重写，L1 fact 保留原始）
- 预估：半天（schema + extract prompt + 测试）

### 3. 哲学借鉴（2 个 — 影响 V6 命名 + V5.5 命名锚定）

| 借鉴点 | 认知科学背书 | V6/V5.5 对应物 | 落地位置 |
|---|---|---|---|
| **3.1 长期记忆 = 自主性的物理基础** | HM 患者案例（无海马体=不能形成新长期记忆=无"自我"）| Hermem 对外定位升级："给 AI 装上长期记忆" / "让 AI 真正记得你 6 个月前说过的话" | 文档/README（不写代码）|
| **3.2 预测编码 + 记忆巩固** | "人脑 90% 神经活动是预测下一步" + "睡眠时大脑把短期记忆转为长期" | V5.5 weekly synthesis（每周日 02:30 跑批处理）= AI 睡眠 | V5.5 SPEC 命名升级 |

**落地动作**：
- 3.1 改 README.md 顶部一句话定位（不写代码，1 h）
- 3.2 V5.5 SPEC.md 把 "weekly synthesis" 改名 "memory consolidation"（轻量隐喻锚定，30 min）

### 4. 不建议借鉴（5 个 — 避免 over-engineering / 场景错位）

| 不借鉴 | 原因 |
|---|---|
| **4.1 HM 患者类比作工程论据** | 生理层面独立 ≠ 工程层面独立；Hermem 已有 disposition/pattern_relevance/recency 各自独立模块 |
| **4.2 目标图谱 G**（status/importance/last_touched/satisfaction）| Memory Box 推"今天该做什么"需要；Hermem 被动 recall 不需要 |
| **4.3 信任账户扣分机制** | Memory Box 主动推送需要；Hermem 被动 recall 不需要 |
| **4.4 跨源实体识别**（"老王"/"老王"/"产品老王" 同一个人）| Memory Box 跨 4 源需要；Hermem 单源（Claude Code sessions）不需要 |
| **4.5 主动推送 / 记忆翻新** | Hermem 是被动 recall，不学推送；但学"在 recall 中体现价值向量" |

### 5. v1.2 Sprint 5 候选（完整任务表，**待 Sprint 0 完成后讨论是否启动**）

| 优先级 | 任务 | 来源 | 内容 | 涉及文件 | 预估 |
|--------|------|------|------|----------|------|
| **P0** | 5.1 | v1.2 借鉴 1.2 | `disposition` conf 自动更新（依赖 5.2）| `phase3/impl/disposition_updater.py` + l1_dispositions schema | 半天 |
| **P0** | 5.2 | v1.2 借鉴 1.2+2.2 | `recall_outcome` 表 + behavior_log hook | `phase3/impl/database.py`（新表）+ `hermes-agent/plugins/memory/hermem/__init__.py` 注入点 hook | 1-2 天 |
| **P0** | 5.3 | v1.2 借鉴 1.3 | L1 fact_extraction 加 `user_role/style/values` 字段 | `phase3/impl/l1_extract.py`（prompt）+ l1_facts schema | 1 天 |
| **P1** | 5.4 | v1.2 借鉴 2.1 | concept_tag 关注度加权（依赖 5.2）| `phase3/impl/vector_search.py`（加权公式）+ concept_weights 字典 | 1 天 |
| **P2** | 5.5 | v1.2 借鉴 2.3 | fact_extraction 保留原始引用（`raw_quote` 字段）| `phase3/impl/l1_extract.py`（prompt）+ l1_facts schema | 半天 |
| **P3** | 5.6 | v1.2 借鉴 3.1 | Hermem 对外定位升级 + V5.5 "weekly synthesis" 改名 "memory consolidation" | `README.md` + `phase3/v5.5/SPEC.md` | 1.5 h |

**Sprint 5 完整预估**：**3-4 天**（一人）

### 6. v1.2 → 最终融合前清单

1. 决策 1（路径）拍板 — v1.0/v1.1 遗留
2. 决策 3（anchor 词典）拍板 — v1.0/v1.1 遗留
3. Sprint 0 跑通（v1.0 + v1.1 任务 0.2-0.9）— 基础设施 bug 全部修掉
4. Sprint 1-4 跑通（V6 主体功能）
5. **Sprint 5 是否启动拍板**（v1.2 新增）：Sprint 0-4 完成后，根据"行为数据价值是否验证"决定
6. Sprint 5 任务 5.1-5.6 优先级拍板：哪些进 Sprint 5 backlog，哪些进 V7+

**融合时机**：决策 1+3 拍板 + Sprint 0-4 跑通 + Sprint 5 启动拍板后

### 7. v1.2 对 v1.0/v1.1 决策待办的影响

| v1.0/v1.1 决策 | 状态 | v1.2 影响 |
|---|---|---|
| 决策 1（路径 phase3/v6/）| 待拍板 | 无影响 |
| 决策 2（0.85 fallback）| ❌ 作废 | 无影响 |
| 决策 3（anchor 词典）| 待拍板 | 无影响 |
| **决策 4**（v1.2 新增）：Sprint 5 是否启动？— **建议** Sprint 0 完成后根据"行为数据价值是否验证"再决定 | 待拍板 |

### 8. v1.2 与 v1.1（MemPalace）的关系

| 维度 | v1.1 MemPalace 借鉴 | v1.2 Memory Box 借鉴 |
|---|---|---|
| 借鉴源 | 开源工具（54k stars, 9 周龄）| C 端产品讨论（2026-06-04）|
| 借鉴层面 | 工程实现（公式、抽象、split）| 方法论（思想、闭环、命名）|
| 落地位置 | Sprint 1-4 任务表（hybrid 公式 / ABC / 50/450）| **Sprint 5 候选**（行为数据 / 长期人格 / 关注度）|
| 优先级 | P0-P2（部分已入 Sprint 1-4）| **P0**（补 Hermem 最大盲点）|
| 风险 | 低（来源透明，代码可读）| 低（C 端产品观察，机制类比）|

**两者互补**：MemPalace 给"V6 怎么做好 RAG 检索"，Memory Box 给"V6 该往哪个方向走"。

---

## v1.3 追加（2026-06-07 — Hindsight 论文借鉴）

**追加背景**：v1.0/v1.1/v1.2 落定后，2026-06-07 与 Oliver 完整评估了 Hindsight（arXiv:2512.12818, vectorize-io/hindsight, 15.9k ★, MIT License, v0.7.2）。论文论证清晰，**但其工程栈（PostgreSQL + pgvector + LLM 抽 narrative）不适合 Hermem 的本地优先 + 轻量定位**。本节只采纳**算法/方法论层**借鉴，**不复制工程实现**。

**关键判断**（与 v1.1 MemPalace / v1.2 Memory Box 借鉴的对比）：

| 借鉴源 | 借鉴层面 | 落地 Sprint | 与 V6 既有方向关系 |
|---|---|---|---|
| v1.1 MemPalace | 工程实现（hybrid 公式、ABC 抽象、split 方法论）| Sprint 1-4 | 已有 |
| v1.2 Memory Box | 方法论（行为数据闭环、长期人格、关注度）| Sprint 5 候选 | 已有 |
| **v1.3 Hindsight** | **检索算法（4 路并行 + RRF 融合 + Temporal 通道 + Reflect 模式）**| **Sprint 1/2/3** | **新增 + 加速** |

### 1. 立即可借鉴（3 个 — 全部加速 V6 已有方向）

#### 1.1 RRF 融合公式（Hindsight 论文公式 15）

| 项 | 详情 |
|---|---|
| 借鉴源 | Hindsight 论文 §4.2.3：RRF(f) = Σ 1/(k+rank_i(f))，k=60 |
| 目的 | 替换 Hermem 当前 `search_with_tier` 的"硬阈值切分"，实现真正的"双路融合" |
| 当前 | `search_with_tier` 用单一 vec 通道 + 分层阈值（HIGH=0.70 / MEDIUM=0.50）|
| 升级 | HNSW 召回的向量候选 ∩ BM25 召回的关键词候选 → 各自排序 → RRF 融合 → 单一分数 |
| 公式 | `rrf_score(d) = 1/(60+rank_vec(d)) + 1/(60+rank_bm25(d))`，未出现在任一通道的 doc 分数为 0 |
| 落地 | **Sprint 1 任务 1.1 增强**（不新增任务，直接替换现有召回逻辑）|
| 涉及文件 | `phase3/impl/vector_search.py` `search_with_tier()` |
| 验收 | 单元测试：同一 doc 出现在两路 vs 仅一路 → 分数差异 ≥ 2x；50 条 ground-truth 上 recall@5 对比旧版 |
| 风险 | RRF 公式简单，调试性差；阈值仍需调（k=60 是否合适需要 sweep）|
| 与 v1.1 MemPalace 关系 | **不冲突**：MemPalace 用 `0.6×vec + 0.4×bm25` 加权融合，RRF 是 rank-based；两者二选一。**建议 RRF**（不需要校准通道分数，Hindsight 论文论证 RRF 在通道分数不可校准时更稳）|

**为什么不是 MemPalace 那种加权融合**：
- 加权融合需要"通道分数可校准"前提，vec cosine ∈ [-1, 1]、BM25 ∈ [0, ∞) —— 单位、分布全不同，必须先归一化
- RRF 只用 rank，绕开校准问题
- Hindsight 论文 §4.2.3 给了 4 条理由（rank-based、robust to missing、no penalty、multi-evidence rise），论证扎实

#### 1.2 Temporal 检索通道（Hindsight 论文 §4.2.2 Temporal Graph Retrieval）

| 项 | 详情 |
|---|---|
| 借鉴源 | Hindsight 论文 §4.2.2：用规则+flan-t5-small 把 "yesterday"/"last weekend"/"June 2024" 解析为 [τ_start, τ_end]，再过滤 fact 的 occurrence 区间 |
| 目的 | 补 Hermem 当前"时间只影响排序权重"—— 改为"时间能过滤召回集" |
| 当前 | `usage_tracker.py` 记录 `last_used_at` 但只做 recency 加权，不做时间区间硬过滤 |
| 升级 | 给 `hermem_search` 加可选 `time_range: tuple[datetime, datetime] \| None` 参数。SQLite 层 `chunks.created_at BETWEEN ?` 直接过滤 |
| 简化版（v1.3）| **不引入 flan-t5-small**（Hindsight 用它兜底规则解析不了的 hard case），Hermem 的查询多数是中文/英文混合时间词，规则 + Python `dateparser` 库（轻量、pip install dateparser）足够 |
| 落地 | **Sprint 1 任务 1.5**（新增）|
| 涉及文件 | `phase3/impl/database.py`（查 chunks 时加 BETWEEN 条件）+ `phase3/impl/retrieval.py`（hermem_search API 加 time_range 参数）+ `hermes-agent/plugins/memory/hermem/__init__.py`（桥接层暴露）|
| 验收 | 单元测试：time_range=(2026-01-01, 2026-03-31) → 只返回 created_at 在区间的 chunk；与 RRF 融合顺序：先 Temporal 过滤再 RRF 融合（不是融合后再过滤）|
| 风险 | dateparser 库约 200KB 依赖；如不愿加依赖可手写 5-6 个 regex（"上周"/"上个月"/"YYYY-MM"/"Q1 2026"）|
| 与 V6 已有方向关系 | 加速 V6 Sprint 1（按需触发）的"anchor 关键词"信号 —— "上次"/"昨天做的"等显式时间词可直接走 Temporal 通道，不必走 anchor 兜底 |

**为什么不抄 Hindsight 的 Graph 通道（spreading activation）**：
- Hindsight 在 PostgreSQL 建完整 entity 图（mention → canonical entity + 4 类边），Hermem 没有 canonical entity 表
- 建 entity 图要先做实体识别（spaCy / LLM），引入额外依赖
- 个人 memory 场景下，**单跳 BM25+vector 融合已经覆盖 80% 召回需求**，entity graph 是 long-tail 优化
- **明确不借鉴进 V6**；进 V7+ backlog

#### 1.3 Reflect 模式按需 API（Hindsight 论文 §5 CARA 简化版）

| 项 | 详情 |
|---|---|
| 借鉴源 | Hindsight 论文 §5：Reflect(B, Q, Θ) → (r, O') —— 调一次 LLM，retrieve + 生成综合答案 + 顺便形成 opinion |
| 目的 | 给 Hermem 加一个"按需深度回忆"模式，类似人脑"主动回忆"（主动搜索记忆 vs 被动触发）|
| 当前 | Hermem 只有 `hermem_search`（返回 chunk），不主动生成答案 |
| 升级 | 新增 `hermem_reflect(query, context_window) -> str`，行为：先 4 路召回（vec+bm25+temporal+rrf）→ top-k 拼 context → 调一次 LLM（Ollama 本地）→ 返回综合答案。**可选**把生成的答案存为 L4 reflection |
| 与 CARA 区别 | **不抄 CARA 的 personality profile (S/L/E/β)** —— Hermem 是个人 memory 不是 character AI，4 维 personality 是 over-engineering。Hermem 用 user_profile.md（L3 画像）做行为引导 |
| 与现有路径关系 | 与 `hermem_search` 平行。`hermem_search` 仍返回 chunks（agent 自己加工），`hermem_reflect` 返回答案（agent 拿到即用）|
| 落地 | **Sprint 3 任务 3.5**（新增，与 V6 Sprint 3 的 Explainable Wrapper 合并实现）|
| 涉及文件 | `phase3/impl/reflect.py`（新）+ `hermes-agent/plugins/memory/hermem/__init__.py`（暴露）|
| 验收 | 单元测试：调 hermem_reflect("上周讨论的 X 进展如何") 返回答案包含 1+ 引用 chunk；LLM 调用超时/失败时降级到 `hermem_search` 返回 chunks |
| 风险 | LLM 调用成本（Ollama 1.5B 也至少 1-2 秒）；与 L4 reflection cron 重复（需明确边界：reflect 是"当下"、L4 reflection 是"批量") |

### 2. 不建议借鉴（4 个 — 避免工程栈/哲学错位）

| 不借鉴 | 原因 |
|---|---|
| **2.1 四网络（World/Experience/Opinion/Observation）**| Hindsight 哲学是"事实 vs 信念"严格分离，配套 personality profile 才能用。Hermem 是个人 memory，没有"信念需要演化"的需求；L0-L4 分层已经做了"原始-聚合-确认-反思"分离，**不是同一抽象** |
| **2.2 CARA personality profile (Skepticism/Literalism/Empathy/β)**| Hindsight 给 AI 配"性格"用于"扮演不同 customer service persona"。Hermem 是单 agent 服务单用户，没有多 persona 需求；用 user_profile.md 引导行为更轻量 |
| **2.3 Opinion reinforcement 公式 26**（c 分数 + α 步长）| Hindsight 用"信念置信度随新证据强化/削弱"。Hermem 是事实库不是 belief 系统，usage_count 已经是"被用过几次"的代理指标，不需要置信度框架 |
| **2.4 LLM 抽 narrative full text 存每条 fact** | Hindsight 每条 fact 都让 LLM 重写成 narrative 段落。Hermem 当前 `l1_extract.py` 已经在做类似事，但**只在 retain 时跑一次** —— 与 Hindsight 一致。问题：Hindsight 论文没给成本数据，但每条 fact 多一次 LLM 调用叠加起来成本可观。Hermem 走 Ollama 本地 1.5B 不在乎 LLM 成本，但**存储膨胀是真实成本**（每条 fact 多 200-500 token narrative），不抄 |

**额外警示**（写在 SPEC 里给 V7+ 的人参考）：
- Hindsight 论文 + README 已经**改写了核心术语**（论文里的 Opinion 在 README 里叫 Mental Models）。任何引用 Hindsight 论文做决策时，**先看当前代码版本，再看论文**。Hermem 不应该有这种版本漂移
- Hindsight 的"evaluation"用 GPT-OSS-120B 作为 judge 评自家答案（论文 §7.2），baseline 数字直接抄对方报告。**Hermem 的评测框架（Sprint 4）必须独立复现、不抄数字**，避免重蹈"自评自 SOTA"

### 3. v1.3 Sprint 任务表

| 优先级 | 任务 | 来源 | 内容 | 涉及文件 | 预估 |
|--------|------|------|------|----------|------|
| **P0** | 1.5 | v1.3 §1.2 | Temporal 检索通道（time_range 过滤 + dateparser 解析）| `phase3/impl/database.py` + `retrieval.py` + 桥接层 | 半天 |
| **P1** | RRF | v1.3 §1.1 | 替换 `search_with_tier` 的硬阈值为 RRF 融合（**作为 Sprint 1.1 增强，不新增编号**）| `phase3/impl/vector_search.py` | 半天 |
| **P1** | 3.5 | v1.3 §1.3 | `hermem_reflect()` 按需 API（与 Sprint 3 可解释包装合并实现）| `phase3/impl/reflect.py`（新）+ 桥接层 | 1 天 |

**v1.3 总预估**：**1.5-2 天**（一人）—— 1.5h 写 RRF、半天 Temporal、1 天 reflect

### 4. v1.3 与 v1.0/v1.1/v1.2 的关系

| 维度 | v1.1 MemPalace | v1.2 Memory Box | v1.3 Hindsight |
|---|---|---|---|
| 借鉴源 | 开源工具（54k stars, 9 周龄）| C 端产品讨论 | 学术论文 + 开源工具（15.9k stars, MIT）|
| 借鉴层面 | 工程实现 | 方法论 | **检索算法** |
| 落地位置 | Sprint 1-4 任务表 | Sprint 5 候选 | **Sprint 1 增强 + Sprint 3 合并** |
| 优先级 | P0-P2 | P0 | **P0-P1** |
| 与 V6 现有方向 | hybrid 公式替换 | 行为数据闭环 | **加速 Sprint 1/3，不新增方向** |
| 风险 | 低 | 低 | **中**（Temporal 通道有依赖管理成本）|

**互补关系**：
- MemPalace 给"V6 怎么做好 RAG 检索"（hybrid 公式）—— **v1.3 改用 RRF 替代 MemPalace 的加权融合**
- Memory Box 给"V6 该往哪个方向走"（行为闭环）
- Hindsight 给"V6 检索管线怎么设计"（4 路并行 + RRF + Temporal + Reflect）—— **架构层的范本**

### 5. v1.3 决策待办

- [ ] **决策 5**：1.1 RRF vs v1.1 MemPalace 加权融合 二选一 —— **建议 RRF**（理由：无需通道分数校准，Hindsight 论文已论证；MemPalace 加权需要先归一化 BM25 分数，工程成本更高）
- [ ] **决策 6**：1.2 Temporal 通道用 dateparser 库 vs 手写 regex —— **建议 dateparser**（200KB 依赖，pip 装，节省 4-6h regex 维护成本；如洁癖可手写 5-6 个 regex，覆盖 80% 用例）
- [ ] **决策 7**：1.3 reflect 模式与 L4 reflection cron 边界 —— **建议 reflect = 即时（用户调时）**、**L4 = 批处理（cron 跑）**，互不替代。reflect 写 L4 标记 `source=reflect_immediate` 区分来源

### 6. v1.3 → 最终融合前清单

1. 决策 1（路径 phase3/v6/）拍板 — v1.0 遗留
2. 决策 3（anchor 词典）拍板 — v1.0 遗留
3. 决策 4（Sprint 5 是否启动）拍板 — v1.2 新增
4. **决策 5/6/7 拍板** — **v1.3 新增**
5. Sprint 0 跑通 — v1.0/v1.1 任务 0.2-0.9
6. Sprint 1-4 跑通（含 1.5 Temporal + RRF 增强 + 3.5 reflect）
7. "行为数据价值是否验证"评估 — v1.2 新增前置
8. Sprint 5 启动拍板 + 5.1-5.6 优先级拍板

**融合时机**：决策 1+3+4+5+6+7 拍板 + Sprint 0-4 跑通 + 行为数据价值评估后

---

*v1.3 追加；v1.0/v1.1/v1.2 草案保留作为决策上下文。最终融合前不删除。*
